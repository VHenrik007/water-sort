[package]
name = "water-sort"
version = "0.1.0"
edition = "2021"

[dependencies]
colored = "2.2.0"
criterion = { version = "0.8.1", features = ["html_reports"] }
rand = "0.9.2"
thiserror = "2.0.9"

[profile.release]
codegen-units = 1
lto = "fat"
opt-level = "z"
panic = "abort"
strip = "symbols"
debug = "line-tables-only"

[[bench]]
name = "water_sort_benchmark"
harness = false